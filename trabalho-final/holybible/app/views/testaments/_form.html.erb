<%= form_with(model: testament, local: true) do |form| %>
  <% if testament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testament.errors.count, "error") %> prohibited this testament from being saved:</h2>

      <ul>
        <% testament.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
